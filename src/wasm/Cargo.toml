[package]
name = "perihelion-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
bonsai-index = { version = "0.1", features = ["wasm"] }
wasm-bindgen = "0.2"

[profile.release]
opt-level = "z"
lto = true
